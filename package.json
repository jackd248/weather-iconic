{
  "name": "weather-iconic",
  "version": "2.0.0",
  "description": "A minimal multi-color weather icon set containing 63 icons offered in multiple integration options.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.esm.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.esm.js",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./sprites": "./dist/sprites.svg",
    "./fonts": "./dist/fonts/",
    "./fonts/css": "./dist/weather-iconic.css",
    "./icon-constants": "./dist/icon-constants.js"
  },
  "files": [
    "dist/**/*",
    "!dist/png/**",
    "!dist/preview.html",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "engines": {
    "node": ">=16.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build:clean": "rimraf dist",
    "build:icons": "node scripts/build-icons.js",
    "build:types": "node scripts/generate-types.js",
    "build:sprite": "node scripts/generate-sprite.js",
    "build:fonts": "node scripts/generate-fonts.js",
    "build:png": "node scripts/generate-png-simple.js",
    "build:vite": "vite build",
    "build": "npm run build:clean && npm run build:icons && npm run build:types && npm run build:vite && npm run build:sprite && npm run build:fonts && npm run build:png",
    "dev": "vite dev",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "icons",
    "weather",
    "svg",
    "sprites",
    "webfont",
    "png",
    "typescript",
    "icon-font"
  ],
  "author": "Konrad Michalik",
  "license": "CC-BY-SA-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/jackd248/weather-iconic.git"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "fantasticon": "^3.0.0",
    "puppeteer": "^21.0.0",
    "rimraf": "^5.0.0",
    "sharp": "^0.33.0",
    "svgo": "^3.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vite-plugin-dts": "^3.0.0",
    "vitest": "^1.0.0"
  }
}
